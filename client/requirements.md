## Packages
framer-motion | Page transitions and animations
embla-carousel-react | For product image galleries and carousels
lucide-react | Icons for the UI

## Notes
- Using localStorage for Cart and Wishlist persistence to ensure fast, optimistic UI updates, syncing with the backend on checkout.
- Assuming the backend serves images from URLs or standard placeholders. 
- Using standard shadcn/ui components imported from `@/components/ui/`.
- Routes rely on `wouter`.
